export const API_BASE_URL = '/choreo-apis/create-your-own-adventure/backend/v1/api'

// replace the above with '/api' for local build. we can configure this later to dynamically choose the correct endpoint.